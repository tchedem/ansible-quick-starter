---

- name: Set system timezone
  hosts: all
  become: true

  vars:
    timezone: Europe/Paris

  tasks:
    - name: Ensure timezone is set
      timezone:
        name: "{{ timezone }}"
      # Using the timezone module is preferred over shell commands
      # because it is idempotent and OS-aware.
