---

- name: Add a line to a file 
  hosts: all
  become: true

  vars:

  tasks:

    # How do I add a line without duplicating it?
    - name: Ensure line exists
      lineinfile:
        path: /etc/example.conf
        line: "enable_feature=true"
        state: present
